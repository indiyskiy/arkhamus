# Release Environment Configuration
# --------------------------------
# This file contains settings specific to the release/production environment

# JPA Configuration
# ----------------
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=create

# Production Mode Settings
# -----------------------
game.testMode=false
game.createTestQuests=false

# Steam Configuration
# ------------------
steam.gameDescription=Cultprits
